# Tooling config only; the app is installed from requirements.txt, not packaged.

[tool.ruff]
line-length = 100
target-version = "py312"  # matches the Dockerfile base image

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]

[tool.ruff.lint.isort]
known-first-party = ["app"]
